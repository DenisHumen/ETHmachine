"""Общие механизмы, которые модули не должны переписывать заново.

- :mod:`modules.core.runner` — параллельный обход кошельков с корректной
  обработкой Ctrl+C и счётчиками.
- :mod:`modules.core.sqlite_store` — работа с SQLite: WAL, отдельное
  соединение на операцию, частичный апдейт, статистика по статусам.

Схемы таблиц и пути к базам остаются за модулями: это состояние
пользователя, менять его нельзя.
"""

from __future__ import annotations

__all__ = ["runner", "sqlite_store"]
