# ========================================================================================
# DEBANK PROTOCOL CHECKER — Конфигурация
# ========================================================================================
# Основные параметры (потоки, ретраи, задержки) — в general_config.py
# Здесь только параметры специфичные для DeBank Protocol Checker

# Минимальная сумма позиции (USD) для записи в файл результатов
# Позиции дешевле этой суммы не попадут в xlsx
MIN_VALUE_USD = 0.10

# Минимальная сумма (USD) для начала цветного градиента ячеек
# Ячейки с суммой ниже этого порога останутся без заливки
GRADIENT_MIN_USD = 0.20

# Максимальная сумма (USD) для полного насыщения градиента
# Ячейки с суммой >= этого значения будут максимально яркими
GRADIENT_MAX_USD = 1000.0
